//package org.joseph

import scala.io.Source

object NlpUtil {
  //def getStopWords(path: String = "src/resources/common.dic": Option[Set[String]]) = {
  //  try {
  //    val stream = getClass.getResourceAsStream(path)
  //    val lines = (for (line <- Source.fromInputStream(stream).getLines()) yield line.trim).toSet
  //    Some(lines)
  //  } catch {
  //    case e: Exception => {
  //      println(e)
  //      None
  //    }
  //  }
  //}
}
